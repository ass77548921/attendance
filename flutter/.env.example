# Flutter Web Docker build/runtime variables
# Copy this file to .env, then adjust values as needed.

# Flutter entrypoint file
FLUTTER_TARGET_FILE=lib/main_stage.dart

# Dart define JSON file (contains API_BASE_URL)
FLUTTER_DEFINE_FILE=flavors/stage.json

# Host port exposed by docker compose
FLUTTER_WEB_PORT=8081
